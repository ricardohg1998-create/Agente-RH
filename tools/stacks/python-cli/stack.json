{
  "id": "python-cli",
  "name": "Python CLI",
  "description": "CLI minima con argparse y empaquetado moderno.",
  "projectType": "cli",
  "copyTargets": [
    "pyproject.toml",
    "src"
  ],
  "postCreateMessage": "Crea un entorno virtual, instala en editable con `python -m pip install -e .` y ejecuta `python -m __PYTHON_PACKAGE__`."
}
