# Changelog

## Registros

- **2026-05-25 (Optimizacion de validacion en Windows 11)**: Corregido el cuello de botella de `tests/Pester/TestHelpers.ps1` al crear workspaces temporales, evitando la copia recursiva de dependencias y caches anidadas como `.agent/mcp/semantic-server/node_modules`. Ajustado `scripts/check-links.ps1` para no validar tokens bajo `node_modules`, alineandolo con la higiene del repo. Aniadida exclusion configurable en `.agent/config/context-budget.json` para referencias externas largas de skills, eliminando el warning de tamano de la suite oficial sin debilitar la capa rapida. Corregida generacion del indice de workflows para emitir texto limpio en Windows y anadida barrera anti-mojibake en `scripts/check-cleanliness.ps1`. Endurecido `scripts/deploy.ps1`: ruta por defecto portable, conservacion de archivos requeridos por el manifiesto y preservacion de `brain/swarm/.gitkeep` en export limpio. Validado export temporal, `init-project.ps1 -InitGit -InstallHook -CreateCatalog -SkipChecks` y `scripts/run-checks.ps1 -KeepGoing` dentro del clon con 32/32 tests verdes y 0 warnings. Documentado el detalle de cambios y motivaciones en `docs/template-hardening-2026-05-25.md`.

- **2026-05-24 (Arquitectura Multiagente con Supervisión y Validación)**: Integrados los nuevos subagentes especializados `SwarmSupervisor` y `QualityValidator` en el Swarm Core v2.0 de Agente RH. Creadas las plantillas dedicadas de prompt de sistema en `.agent/templates/` para el Supervisor y para el Validador de Calidad (este último incorporando crítica intelectual rigurosa y constructiva del código). Actualizado el protocolo operativo en `.agent/rules/swarm-orchestration.md` integrando los nuevos roles y el flujo de callbacks interactivo (`[BLOCKED]`, `[QA_PENDING]`, `[REFACT_NEEDED]`, `[APPROVED]`). Modificado el script orquestador `scripts/spawn-swarm.ps1` para soportar dinámicamente estas plantillas y checklists de tareas específicos, e inyectarlos de forma automatizada en enjambres de desarrollo concurrente. Sincronizada la documentación de arquitectura (`brain/architecture.md` y `brain/skills-available.md`) con la suite de validación general `run-checks.ps1` en verde absoluto (31/31 tests pasando con éxito).
- **2026-05-22 (Portabilidad, DRY e Higiene Global)**: Consolidada la suite de portabilidad mediante la eliminación de todas las rutas de archivo absolutas (`file:///r:/Escritorio/...`) en workflows y reglas, sustituyéndolas por enlaces markdown relativos estables. Aplicada una política estricta DRY (Don't Repeat Yourself) en memoria operativa, convirtiendo `workflows-index.md` en un índice dinámico autogenerado con links portables y redirigiendo `skills-available.md` a `CATALOG.md`. Robustecida la arquitectura de enjambres Swarm v2.0 con el protocolo de escape ante conflictos de Git, purga higiénica física de `brain/swarm/` integrada al cierre operativo, exclusión de `brain/swarm/` en el presupuesto de contexto, y conversión a rutas dinámicas `$env:USERPROFILE` en `spawn-swarm.ps1`. Normalizada la ortografía y codificación UTF-8 de los 13 workflows core y validada la suite completa de tests de integración con 31/31 tests Pester y checks en verde absoluto.
- **2026-05-22 (Swarm Scaffolder & Resiliencia MCP)**: Diseñado e implementado el automatismo ejecutable de Swarms mediante `spawn-swarm.ps1` con aislamiento de trabajo higiénico en `brain/swarm/` y alineación conceptual a través de `swarm-bootstrap-template.md`. Blindada la DX del Semantic Server mediante Smoke Test dinámico de 1.2 segundos en `install-mcp.ps1` (robusto ante espacios en el path) y degradación elegante en `init-project.ps1` capturando fallos de compilación e inyectando `semantic_server_active: false` en `stack.md` para desinhibir con seguridad el uso de `grep_search`. Erradicados de raíz los problemas de mojibake (caracteres corruptos) en la consola de Windows PowerShell mediante el saneamiento y remoción de diacríticos y acentos en todas las directivas de salida a pantalla (`Write-Host`, `Write-Warning`, `Write-Error`).
- **2026-05-22 (Auditoría Profunda Autista Cafeinado)**: Finalizada con éxito absoluto la auditoría de mejoras en DX, robustez y performance. Corregido el bug de escapes Markdown en `generate-workflows-docs.ps1`, eliminada duplicidad semántica con el módulo `scripts/lib/link-utils.psm1`, blindados todos los commandlets de sistema de archivos usando `-LiteralPath` contra paths complejos, forzada la codificación UTF-8 sin BOM para evitar mojibakes en Windows 11. Optimizados los tests unitarios de Pester (reduciendo de 169s a 154s con una lista negra de exclusión en workspaces de tests). Sanitizado regex contra inyecciones en `brain-utils.psm1`, archivado script obsoleto `update-template.ps1`, corregido bug de parser error en el validador de links y armonizadas reglas operativas. Suite de checks locales 100% en verde.
- **2026-05-22**: Migración a Antigravity 2.0 & Swarm Core v2.0 — Adaptación y blindaje quirúrgico completo del repositorio para compatibilidad con Gemini 3.5 Flash y Antigravity 2.0 en Windows 11. Refactorización de todos los workflows de desarrollo para soportar enjambres asíncronos en workspaces concurrentes. Corrección de falsos positivos en `check-links.ps1` con rutas de URI `file:///` decodificadas nativamente mediante `[System.Uri]`. Creación de plantillas DX e higienización de artefactos efímeros en la exportación virgen del template. 22/22 tests de integración Pester verdes.
- **2026-04-13**: Alineación de herramientas e instrucciones — Modificados `AGENTS.md` y `.agent/rules/core.md` para hacer mandatory el uso del servidor MCP Semántico nativo frente a grep y obligar al agente a usar la sección oculta `<skills>` aportada por el environment (Antigravity). Actualizado `brain/skills-available.md`.
- **2026-04-11**: Auditoría Autista Cafeinado completada — 22/22 tests Pester verdes. 6 bugs críticos resueltos (sync-brain marcadores/parsing, check-links scoping/filtros, bootstrap directorio padre, init-project hang). Renombrado `Normalize-Eol` a `ConvertTo-NormalizedEol` en módulo exportado. Deuda técnica documentada exhaustivamente.
- **2026-04-10**: Saneamiento MCP y Factory Reset — Eliminada contaminación de dominio (`social-content`). Factory Reset en `init-project.ps1` para clones limpios. Servidor Semántico MCP integrado (`ts-morph`) con autoinstalación. Corregido encoding UTF-8 BOM en `install-mcp.ps1`.
- **2026-04-05**: Workflows v3.0 — 13 workflows operativos (10 mejorados + 3 nuevos). Herramientas sugeridas Antigravity-native, composicion y encadenamiento, modos de operacion, dispatch con senales ricas, meta-sistema de metricas. Infraestructura actualizada (create-workflow.ps1, brain-policy.json, implementation-plan-template.md, repo-structure.json).
- **2026-04-03**: Mejora integral de workflows v2.0 — Reescritura profunda de `autista-cafeinado` y `desarrollador-profundidad`. Nuevos workflows `code-review` y `retrospectiva`. Fix de `create-workflow.ps1`. Campo `description` en todos.
- **2026-03-21**: Finalizado el retroceso de mejoras (Backport) desde el proyecto derivado `comparador v2`. Añadida la sección de infraestructura a `AGENTS.md`, creado placeholder en `brain/access.md`, reparados índices faltantes en el catálogo base y relajadas las normas de higiene de logs (`*.log`, `*.bak`).
- **2026-03-20**: Instalación exitosa de `Agentic-SEO-Skill` en el workspace de habilidades del agente (seo). Se ejecutó una validación funcional auditando el sitio `conectayaentuhogar.es`, generando un dashboard en HTML, un Full Audit Report y un Action Plan.
- 2026-05-24 | Decision registrada (DEC-20260524-005147): Endurecer plantilla Antigravity Desktop 2.0
